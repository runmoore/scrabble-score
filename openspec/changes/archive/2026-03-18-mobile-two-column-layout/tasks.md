## 1. Layout Change

- [x] 1.1 Change `grid-cols-1` to `grid-cols-2` on the stats card grid in `app/routes/games/players.$playerId.tsx`

## 2. Verification

- [x] 2.1 Verify cards render correctly at mobile (375px), tablet (768px), and desktop (1920px) viewports
